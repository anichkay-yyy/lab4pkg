@startuml activity_login
title Диаграмма деятельности — Аутентификация

start
:Открыть приложение;
:Ввести логин;
:Ввести пароль;
:Вычислить хэш пароля;
:Поиск пользователя в БД;

if (Пользователь найден?) then (да)
    if (Хэш совпадает?) then (да)
        :Определить роль пользователя;
        if (Роль = admin?) then (да)
            :Показать меню администратора;
        else (нет)
            :Показать меню маклера;
        endif
    else (нет)
        :Сообщение: "Неверный пароль";
        stop
    endif
else (нет)
    :Сообщение: "Пользователь не найден";
    stop
endif

stop
@enduml
